---
name: 10x-new
description: Initialize a new change folder under context/changes/<change-id> with a change.md identity file
---

# /10x-new — Rozpocznij nową zmianę

Inicjuje nowy folder zmiany w `context/changes/<change-id>/`. Tworzy mały plik tożsamości (`change.md`) i kieruje użytkownika do następnej umiejętności.

„Zmiana” to pojedyncza jednostka pracy od początku do końca — badania, planowanie, implementacja
